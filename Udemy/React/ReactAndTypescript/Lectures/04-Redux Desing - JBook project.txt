Redux Design: cells and bundles reducers
	
	Action Creators						|					Redux Store													|
										|																				|
		updateCell						|					data {[cell id]: Cell}  array of cells						|
		deleteCell						|		cells		loading boolean 		true/false when fetching data		|	
		insertCellBefore/After	---->	|					error string | 	 		null errors related to saving cells |
		moveCell						|					order string[] 			order of cells						|
		fetchCells						|																				|
										|		bundles		data {[cell id]: Bundle} 	bundle for each cell			|
										
						
Cells

	State:
	
		{
			loading: false,
			error: null,
			data: {
				'idxxxxx': { id: idxxxxx, type: 'code', content: 'const a = 1;' }
			}
		},
		{	
			loading: false,
			error: null,
			data: {
				'id2xxxxx': { id: id2xxxxx, type: 'text', content: 'Documentation for this thing...' }
			}
		},		




Create this structure:

	C:\USERS\OSANTAMARIA\GIT\REACTANDTYPESCRIPT\JBOOK\SRC\STATE
	│   cell.ts
	│   index.ts
	│   store.ts
	│
	├───action-creators
	│       index.ts
	│
	├───action-types
	│       index.ts
	│
	├───actions
	│       index.ts
	│
	└───reducers
			bundlesReducer.ts
			cellsReducer.ts
			index.ts


						
1. Create ActionTypes first

2. Create Actions										

3. Create the reducers

4. Configure the store

5. Wire up everything to the React side (src/index.tsx)

6. Tackle Action Creators


Example:
	
	import { ActionType } from "../action-types";
	import { Action } from "../actions";
	import { Cell } from "../cell";

	interface CellsState {
		loading: boolean;
		error: string | null;
		order: string[];
		data: {
			[key: string]: Cell;
		};
	}

	const initialState: CellsState = {
		loading: false,
		error: null,
		order: [],
		data: {},
	};

	/*
		case ActionType.UPDATE_CELL: Update the content of a cell—identified by its id—in the data object of the state without mutating the original state.

			case ActionType.UPDATE_CELL:
				return {
					...state,
					data: {
						...state.data,
						[action.payload.id]: {
							...state.data[action.payload.id],
							content: action.payload.content,
						},
					},
				};

				refactor:

					case ActionType.UPDATE_CELL:
						const { id, content } = action.payload;
							return {
								...state,
								data: {
									...state.data,
									[id]: {
										...state.data[id],
										content
									},
								},
							};


		- return {
			...state,
	 
			You're returning a new state object. Using the spread operator ...state ensures you're copying everything from the current state 
			(like loading, error, and order) into the new one.

		- data: {
			...state.data,
			
			You're copying all existing cells in the data object—this maintains immutability (you’re not modifying the original object directly).

		- [action.payload.id]: {
				...state.data[action.payload.id],
				content: action.payload.content,
			}

			Now here’s where the update happens:

				1. [action.payload.id]: This dynamically selects the cell by ID.

				2. ...state.data[action.payload.id]: Copies the existing properties of that cell (like id, type, etc.).

				3. content: action.payload.content: Overrides the existing content with the new content from the action.

		Example: 
		
			Let’s say your state looks like this:
		
				data: {
					"abc123": { id: "abc123", type: "code", content: "old content" }
				}

			And your action is:

				{
					type: "update_cell",
					payload: { id: "abc123", content: "new content" }
				}

			After this reducer runs, the updated state will be:

				data: {
					"abc123": { id: "abc123", type: "code", content: "new content" }
				}

		

	*/
	const reducer = (
		state: CellsState = initialState,
		action: Action
	): CellsState => {
		switch (action.type) {
			case ActionType.UPDATE_CELL:
				const { id, content } = action.payload;
				return {
					...state,
					data: {
						...state.data,
						[id]: {
							...state.data[id],
							content,
						},
					},
				};
			case ActionType.DELETE_CELL:
				return state;
			case ActionType.MOVE_CELL:
				return state;
			case ActionType.INSERT_CELL_BEFORE:
				return state;
			default:
				return state;
		}
	};

	export default reducer;


This is very complex, that's why we want to use immer

	const reducer = (
		state: CellsState = initialState,
		action: Action
	): CellsState => {
		switch (action.type) {
			case ActionType.UPDATE_CELL: {
				const { id, content } = action.payload;
				// using immer
				state.data[id].content = content;
				return state;
			}
				
					
			case ActionType.DELETE_CELL:
				return state;
			case ActionType.MOVE_CELL:
				return state;
			case ActionType.INSERT_CELL_BEFORE:
				return state;
			default:
				return state;
		}
	};
	
7. Use the store with a Provider in your App
	
	import "bulmaswatch/superhero/bulmaswatch.min.css";
	import ReactDOM from "react-dom/client";
	//import CodeCell from "./components/code-cell";
	//import TextEditor from "./components/text-editor";
	import { Provider } from "react-redux";
	import { store } from "./state";
	import CellList from "./components/cell-list";

	const root = ReactDOM.createRoot(
		document.getElementById("root") as HTMLElement
	);

	const App = () => {
		return (
			<Provider store={store}>
				<div>
					<CellList />
				</div>
			</Provider>
		);
	};

	root.render(<App />);
	
8. Create useAppSelector and useAppDispatch hooks


Next steps in the project:

- The user will type a command from terminal like npx jbook serve. 

- I need some kind of cli that will start a local node api (server). 

- This server has 3 jobs: 
	
	1. Allow the user to go to localhost:4050 and get our build react project files. 
	2. Write any changes that the user makes to any cells to a file on a users machine (notebook.js)
	3. Load up that file and provide a list of all the cells over to the react app
	
	
CLI: 	
	- Needs to know how to start up the local API.
	- Needs to know how to publish a notebook to the public API.
	- jbook

Local Express API
		
	- Needs to serve up the react app.
	- Needs to be able to save/load cells from a file.
	- @jbook/local-api
	
Public Express API
		
	- Needs to serve up the react app.
	- Needs to be able to save/load cells from a DB.
	- Needs to handle auth/permissions etc.
	- @jbook/public-api
	
React App	

	- Needs to make its production assets available to either the local API or the Public API
	- @jbook/local-client
	
Lerna: Tool for managing multipackage projects.
	
	1. Install lerna: npm install -g --save-exact lerna@3.22.1
	1. Rename the jbook project folder to local-client
	2. Create another jbook folder.
	3. Inside the new jbook run the command:
		
		lerna init
		
		output:
		
			lerna notice cli v3.22.1
			lerna info Creating package.json
			lerna info Creating lerna.json
			lerna info Creating packages directory
			lerna success Initialized Lerna files
			
	4. Move the local-client folder inside jbook/packages folder
	
	5. Inside jbook folder:
		
		mkdir cli
		mkdir local-api
		
	6. Inside cli foler
		
		npm init -y
		
	7. Inside local-api
		
		npm init -y
		
		
	8. lerna add commander --scope=cli  // popular Node.js library for building command-line interfaces (CLIs)
	
	9. Inside local-api create an index.js and add
		
		module.exports = () => {
			console.log('Server started');
		};
		
	10. learna add local-api --scope=cli
	
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\local-api> lerna add local-api --scope=cli
		lerna notice cli v3.22.1
		lerna notice filter including "cli"
		lerna info filter [ 'cli' ]
		lerna info Adding local-api in 1 package
		lerna info Bootstrapping 3 packages
		lerna info Symlinking packages and binaries
		lerna success Bootstrapped 3 packages

	11. Inside cli create and index.js with this:
		
			const serve = require('local-api');
			serve();
			
	12. Run the command: 
		
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> node index.js
		Server started
		
	13. lerna add typescript --dev --scope=local-api
	
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> lerna add typescript --dev --scope=local-api
		lerna notice cli v3.22.1
		lerna notice filter including "local-api"
		lerna info filter [ 'local-api' ]
		lerna info Adding typescript in 1 package
		lerna info Bootstrapping 3 packages
		lerna info Installing external dependencies
		lerna info Symlinking packages and binaries
		lerna success Bootstrapped 3 packages
	
	14. PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\local-api> npx tsc --init

		Created a new tsconfig.json with:
																															 TS
		  target: es2016
		  module: commonjs
		  strict: true
		  esModuleInterop: true
		  skipLibCheck: true
		  forceConsistentCasingInFileNames: true
	
	15. Delete local-api\index.js
	
	16. Create an src folder inside local-api and then an index.ts
		
		export default () => {
			console.log('Server is listening');
		}	
	
	17. Modify tsconfig.json: 
		
		"declaration": true,  
		"outDir": "./dist", 

	18. Add a new script in package.json

		"scripts": {
			"start": "tsc --watch --preserveWatchOutput",
			"test": "echo \"Error: no test specified\" && exit 1"
		},
		
	19. Inside local-api run: npm run start
	
	20. Inside local-api, edit package.json. This line "main": "dist/index.js". Then add another line: "types": "dist/index.d.ts"
	
	21. Inside cli folder create an src and move the index.js into the folder.
	
	22. Modify index.js into index.ts with this code:
		
		import serve from 'local-api';
		serve();
		
	23. Stop the process running on terminal and switch to cli folder.
	
	24. PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> lerna add typescript --dev --scope=cli
	
		lerna notice cli v3.22.1
		lerna notice filter including "cli"
		lerna info filter [ 'cli' ]
		lerna info Adding typescript in 1 package
		lerna info Bootstrapping 3 packages
		lerna info Installing external dependencies
		lerna info Symlinking packages and binaries
		lerna success Bootstrapped 3 packages

	25. npx tsc --init
	
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> npx tsc --init

		Created a new tsconfig.json with:
																															 TS
		  target: es2016
		  module: commonjs
		  strict: true
		  esModuleInterop: true
		  skipLibCheck: true
		  forceConsistentCasingInFileNames: true
		  
	26. Inside cli folder open tsconfig.json -> "outDir": "./dist", 
	
	27. Look for package.json and remove main and add script:
		
		"scripts": {
			"start": "tsc --watch --preserveWatchOutput"
		},
		
	28. npm run start
		
			PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> npm run start

			> cli@1.0.0 start
			> tsc --watch --preserveWatchOutput

			[3:24:25 PM] Starting compilation in watch mode...

			[3:24:25 PM] Found 0 errors. Watching for file changes.
			
	29. node index.js
	
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli> cd .\dist\

		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js
		Server is listening
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist>
		
	30. In jbook\package.json
		
		"scripts": {
			"start": "lerna run start --parallel"
		}
		
	31. PS C:\Users\osantamaria\git\ReactAndTypescript\jbook> npm run start

		> start
		> lerna run start --parallel

		lerna notice cli v3.22.1
		lerna info Executing command in 3 packages: "npm run start"
		(node:11432) [DEP0060] DeprecationWarning: The `util._extend` API is deprecated. Please use Object.assign() instead.
		(Use `node --trace-deprecation ...` to show where the warning was created)
		cli: > cli@1.0.0 start
		cli: > tsc --watch --preserveWatchOutput
		local-api: > local-api@1.0.0 start
		local-api: > tsc --watch --preserveWatchOutput
		jbook: > jbook@0.1.0 start
		jbook: > react-scripts start
		cli: 4:06:29 PM - Starting compilation in watch mode...
		local-api: 4:06:29 PM - Starting compilation in watch mode...
		cli: 4:06:29 PM - Found 0 errors. Watching for file changes.
		local-api: 4:06:29 PM - Found 0 errors. Watching for file changes.
		jbook: Starting the development server...
		
	32. Inside the cli\src folder create a commands folder and create a serve.ts file
	
	33. Replace index.ts with 
		
		import { program } from "commander";
		import { serveCommand } from "./commands/serve";

		program.addCommand(serveCommand);
		
	34. PS C:\Users\osantamaria\git\ReactAndTypescript\jbook> lerna add @types/node --dev --scope=cli
		lerna notice cli v3.22.1
		lerna notice filter including "cli"
		lerna info filter [ 'cli' ]
		lerna info Adding @types/node in 1 package
		lerna info Bootstrapping 3 packages
		lerna info Installing external dependencies
		lerna info Symlinking packages and binaries
		lerna success Bootstrapped 3 packages
		
	35. Make sure PS C:\Users\osantamaria\git\ReactAndTypescript\jbook> npm run start then
	
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve
		Getting ready to serve a file
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist>	
		
	36. Update the command file
	
		import { Command } from "commander";

		export const serveCommand = new Command()
			.command("serve [filename]")
			.description("Open a file for editing")
			.option("-p, --port <number>", "port to run server on", "4005")
			.action(() => {
				console.log("Getting ready to serve a file");
			});
			
	37. Try PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve --help
			
			Usage: index serve [options] [filename]

			Open a file for editing

			Options:
			  -p, --port <number>  port to run server on (default: "4005")
			  -h, --help           display help for command
			  
	38. Update the file
		
		import { Command } from "commander";

		export const serveCommand = new Command()
			.command("serve [filename]")
			.description("Open a file for editing")
			.option("-p, --port <number>", "port to run server on", "4005")
			.action((filename = 'notebook.js', options) => {
				console.log(filename, options);
			});
	
	39. Try:
		
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve
		notebook.js { port: '4005' }
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve book.js
		book.js { port: '4005' }
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve book.js --port 5000
		book.js { port: '5000' }
		PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve -p 5000 book.js
		book.js { port: '5000' }
		
	40. Communication from the CLI to the API
	
		Remember to PS C:\Users\osantamaria\git\ReactAndTypescript\jbook> npm run start here to start parallel execution
	
		In local-api\src\index.js
			
			export const serve = (port: number, filename: string, dir: string) => {
				console.log('serving traffic on port', port);
				console.log('saving/fetching cells from', filename);
				console.log('that file is in dir', dir);
			};
		
		In cli\src\commands\serve.ts
			
			import path from "node:path";
			import { Command } from "commander";
			import { serve } from 'local-api';

			export const serveCommand = new Command()
				.command("serve [filename]")
				.description("Open a file for editing")
				.option("-p, --port <number>", "port to run server on", "4005")
				.action((filename = 'notebook.js', options: { port: string }) => {
					const dir = path.join(process.cwd(), path.dirname(filename));
					serve(parseInt(options.port), path.basename(filename), dir);
				});

		run: PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve notes/notebook.js
			
			serving traffic on port 4005
			saving/fetching cells from notebook.js
			that file is in dir C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist\notes
			
		run: PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist> node index.js serve notebook.js      
			
			serving traffic on port 4005
			saving/fetching cells from notebook.js
			that file is in dir C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages\cli\dist
		
	41. Adding a local-only api
		
		Adding dependencies and running express
		
		- PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages> lerna add express --scope=local-api
		- PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages> lerna add @types/express --dev --scope=local-api
		- PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages> lerna add cors --scope=local-api
		- PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages> lerna add @types/cors --dev --scope=local-api
		- PS C:\Users\osantamaria\git\ReactAndTypescript\jbook\packages> lerna add http-proxy-middleware --scope=local-api
		
		Inside local-api\src\index.ts
			
			import express from 'express';

			export const serve = (port: number, filename: string, dir: string) => {
				const app = express();

				return new Promise<void>((resolve, reject) => {
					app.listen(port, () => {
						resolve();
					}).on('error', reject);
				});
			};
		
		Inside  cli\src\commands\serve.ts
			
			import path from "node:path";
			import { Command } from "commander";
			import { serve } from "local-api";

			export const serveCommand = new Command()
				.command("serve [filename]")
				.description("Open a file for editing")
				.option("-p, --port <number>", "port to run server on", "4005")
				.action(async (filename = "notebook.js", options: { port: string }) => {
					try {
						const dir = path.join(process.cwd(), path.dirname(filename));
						await serve(parseInt(options.port), path.basename(filename), dir);
						console.log(`Opened ${filename}. Navigate to http://localhost:${options.port} to edit the file.`)
					} catch (err) {
						if (err instanceof Error && typeof (err as any).code === "string") {
							if ((err as any).code === "EADDRINUSE") {
								console.error(
									"Port is in use. Try running on a different port."
								);
							} else {
								console.log("Here is the problem:", err.message);
							}
						} else {
							console.log("Unexpected error:", err);
						}
						process.exit(1);
					}
				});
				
		Accessing the react app


		